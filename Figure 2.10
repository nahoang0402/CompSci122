#include <stdio.h>
#include <math.h>

//Created by Nathan Hoang
//Project 1 - Figure 2.10
//Date: October 1, 2020

int main() {
    int num;
    int n=0;
    printf("Enter a number base (2, 3, 8, 10 or 16):\n");
    scanf("%d", &num);
    if (num == 2) {
        while (n!=4){
            printf("%f\n", pow(num, n));
            n++;
        }
    }
    else if (num == 3){
        while (n!=4){
            printf("%f\n", pow(num, n));
            n++;
        }
    }
    else if (num == 8){
        while (n!=4){
            printf("%f\n", pow(num, n));
            n++;
        }
    }
    else if (num == 10){
        while (n!=4){
            printf("%f\n", pow(num, n));
            n++;
        }
    }
    else if (num == 16){
        while (n!=4){
            printf("%f\n", pow(num, n));
            n++;
        }
    }
    else{
        printf("Error please input 2, 3, 8, or 16");
    }
    return 0;
}
